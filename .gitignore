# Outputs (too large for git)
output/*.mp4
output/*.mp3
output/*.aac
assets/fog_loop.mp4
__pycache__/
*.pyc

# Source assets (copyright risk + size)
audio/queue/*
audio/used/*
audio/*.mp3
audio/*.wav
audio/*.flac
images/queue/*
images/used/*

# Keep folder structure markers
!audio/.gitkeep
!audio/queue/.gitkeep
!audio/used/.gitkeep
!images/.gitkeep
!images/queue/.gitkeep
!images/used/.gitkeep
!output/.gitkeep
!descriptions/.gitkeep

# Descriptions are personal — keep template only
descriptions/*.txt
!descriptions/template.txt

# OAuth credentials (NEVER commit)
client_secrets.json
*.token
.env
.env.local

# OS junk
.DS_Store
Thumbs.db
*.swp
*~

# Logs
*.log

# Scheduler state (per-machine)
.schedule_state
.batch_state

# Personal queue (keep example only)
queue.csv
